project(
  'keccc',
  'c',
  version: '0.1.0',
  meson_version: '>=0.60.0',
  default_options: [
    # 'buildtype=debug',
    # 'b_sanitize=address,undefined',
  ],
)

# Add the source subdirectory containing the executable
subdir('src')
subdir('tests')
